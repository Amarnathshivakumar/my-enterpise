{
  "name": "template-vitev1",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "init": "npm install --prefix backend && pnpm install --prefix frontend",
    "stack": "concurrently \"pnpm run dev --prefix frontend\" \"npm run start:dev --prefix backend\"",
    "build": "concurrently \"pnpm run build --prefix frontend\" \"npm run build --prefix backend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
